# Policy Fixture

This fixture exists to make the Core-v0 boundary executable in repository
metadata and acceptance inventory.

It intentionally exercises legacy comment-like script text that belongs to the
full HTML script-data state family, not the Core-v0 script text-mode subset.
