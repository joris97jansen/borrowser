[package]
name = "html-internal-consumer-probe"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
html = { path = "../../..", default-features = false, features = ["html5", "internal-api"] }
