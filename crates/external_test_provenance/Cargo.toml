[package]
name = "external-test-provenance"
version = "0.1.0"
edition = "2024"

[dependencies]
ring = "0.17.14"
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"

[dev-dependencies]
tempfile = "3"

[lib]
name = "external_test_provenance"
path = "src/lib.rs"
