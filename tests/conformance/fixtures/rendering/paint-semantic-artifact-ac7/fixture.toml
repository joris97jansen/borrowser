format = "borrowser-conformance-fixture-v2"
id = "paint-semantic-artifact-ac7"
scope = "static-html-css-no-js"
observation = "paint-operations"
test_path = "rendering/document.html"

[source]
kind = "native"

[metadata]
description = "Paint-owned semantic artifact content, excluding AC retained lifecycle state."

[execution_package]
entry_path = "rendering/fixture.toml"
support_paths = [
  "rendering/author.css",
  "rendering/expected-01.txt",
]
