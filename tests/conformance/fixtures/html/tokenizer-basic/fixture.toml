format = "borrowser-conformance-fixture-v2"
id = "html-tokenizer-basic-document"
scope = "static-html-css-no-js"
observation = "html-tokenizer"
test_path = "parser/input.html"

[source]
kind = "native"

[metadata]
description = "Reviewed ordinary character data through the canonical tokenizer observation profile."

[execution_package]
entry_path = "parser/fixture.toml"
support_paths = ["parser/parse-errors.txt", "parser/tokens.txt"]
