format = "borrowser-conformance-fixture-v2"
id = "css-selector-matching-parser-dom"
scope = "static-html-css-no-js"
observation = "css-selectors"
test_path = "css/selectors.txt"

[source]
kind = "native"

[metadata]
description = "AF selector matching seed over canonical parser-created DOM and document mode."

[execution_package]
entry_path = "css/fixture.toml"
support_paths = ["css/document.html", "css/expected.txt"]
