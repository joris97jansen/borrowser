format = "borrowser-conformance-fixture-v2"
id = "css-cascade-basic-author-rule"
scope = "static-html-css-no-js"
observation = "css-cascade"
test_path = "css/input.css"

[source]
kind = "native"

[metadata]
description = "Strict AG5 refinement of the original AG2 author-rule cascade seed: the class selector wins over the type selector through AF's production winner-selection path."

[execution_package]
entry_path = "css/fixture.toml"
support_paths = ["css/document.html", "css/expected.txt"]
