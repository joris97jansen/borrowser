format = "borrowser-conformance-fixture-v2"
id = "css-selector-parsing-basic-list"
scope = "static-html-css-no-js"
observation = "css-selectors"
test_path = "css/selectors.txt"

[source]
kind = "native"

[metadata]
description = "Replacement for the AG2 stylesheet inventory seed: direct selector-list input is a distinct logical case required by the phase-isolated AG5 parser profile."

[execution_package]
entry_path = "css/fixture.toml"
support_paths = ["css/expected.txt"]
